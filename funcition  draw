function draw() {
let corFundo =lerpColor(color(217, 112,26), color(219,239,208,)
map(totalArvore, 0, 100, 0, 1));
background(corFundo);
mostrarInformacoes();
temperatura +=0.1;
jardineiro.atualizar();
jardineiro.mostrar();
verificarFimDejogo();
 plantas.map((arvore) => arvore.mostrar();
}
function mostrarInformacoes(){
